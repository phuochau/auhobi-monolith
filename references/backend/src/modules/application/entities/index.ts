import { ApiKey, ApiKeyDTO } from "./api-key.entity"
import { Application, ApplicationDTO } from "./application.entity"

export const Entities = [
   ApiKey,
   Application
]

export const DTOs = [
    ApiKeyDTO,
    ApplicationDTO
]